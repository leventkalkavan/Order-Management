@model List<OrderManagement.Web.DTOs.FeatureWebDto.ResultFeatureWebDto>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/AdminLayout/Index.cshtml";
}

<div class="content">
    <div class="container-fluid">
        <h4 class="page-title">Öne Çıkan İşlemleri</h4>
        <div class="row">
            <div class="col-md-12">
                <div class="card">
                    <div class="card-header">
                        <div class="card-title">Öne Çıkan Listesi</div>
                    </div>
                    <div class="card-body">
                        <table class="table mt-3">
                            <thead>
                            <tr class="text-center">
                                <th scope="col">Öne Çıkan Id</th>
                                <th scope="col">Başlık 1</th>
                                <th scope="col">Başlık 2</th>
                                <th scope="col">Başlık 3</th>
                                <th scope="col">Açıklama 1</th>
                                <th scope="col">Açıklama 2</th>
                                <th scope="col">Açıklama3</th>
                                <th scope="col">Güncelle</th>
                                <th scope="col">Sil</th>
                            </tr>
                            </thead>
                            <tbody>
                            @foreach (var item in Model)
                            {
                                <tr class="text-center">
                                    <td>@item.Id</td>
                                    <td>@item.Title</td>
                                    <td>@item.Title2</td>
                                    <td>@item.Title3</td>
                                    <td>@item.Description</td>
                                    <td>@item.Description2</td>
                                    <td>@item.Description3</td>
                                    <td>
                                        <a href="/Features/UpdateFeature/@item.Id">
                                            <img src="@Url.Content("~/img/edit.png")" alt="Edit Image" width="20" height="20">
                                        </a>
                                    </td>
                                    <td>
                                        <a href="/Features/DeleteFeature/@item.Id"
                                           onclick="return confirm('Silmek istediğinize emin misiniz?')">
                                            <img src="@Url.Content("~/img/delete.png")" alt="Delete Image" width="20" height="20">
                                        </a>
                                    </td>
                                </tr>
                            }
                            </tbody>
                        </table>
                        <a href="/Features/CreateFeature" class="btn btn-outline-primary">Öne Çıkan Ekle</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>